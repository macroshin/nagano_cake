<h3>商品詳細</h3>

<%= attachment_image_tag @item, :image, format: 'jpeg', size: "40x40" %>
<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>商品説明</th>
      <th>ジャンル</th>
      <th>税込価格<br>(税抜価格)</th>
      <th>販売ステータス</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @item.name %></td>
      <td><%= @item.introduction %></td>
      <td><%= @item.genre.name %></td>
      <td><%= @item.price + @item.price*0.1 %>(<%= @item.price %>)円</td>
      <td><%= @item.is_active_i18n %></td>
    </tr>
  </tbody>
</table>
<%= link_to "編集する", edit_admin_item_path(@item) %>